{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <article class="col-md-12">
        <div class="page-header">
            <a href="{{ path('role_index') }}" class="btn btn-success pull-right">Retour</a>
            <h1>Ajouter un rôle</h1>
        </div>
        <div class="well">

            {{ form_start(form) }}
                <div class="form-group">
                    {{ form_widget(form) }}
                </div>
                <input class="btn btn-success" type="submit" value="Ajouter" />
            {{ form_end(form) }}
        </div>
    </article>
</div>
{% endblock %}
