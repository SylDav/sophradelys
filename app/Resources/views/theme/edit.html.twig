{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <article class="col-md-12">
            <div class="page-header">
                <a href="{{ path('user_historic', {'id': theme.user.id }) }}" class="btn btn-success pull-right">Retour</a>
                <h1>Modifier le thème &laquo; {{ theme }} &raquo;</h1>
            </div>
            <div class="well">

                {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <input class="btn btn-success" type="submit" value="Modifier" />
                {{ form_end(edit_form) }}
            </div>
        </article>
    </div>
{% endblock %}
